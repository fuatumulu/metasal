<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MetaSal - Yorumlar</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="sidebar">
        <div class="sidebar-header">
            <h2>MetaSal</h2>
        </div>
        <ul class="nav-links">
            <li>
                <a href="/dashboard">📊 Dashboard</a>
            </li>
            <li>
                <a href="/accounts">👤 Hesaplar</a>
            </li>
            <li>
                <a href="/targets">🎯 Hedefler</a>
            </li>
            <li>
                <a href="/posts">📝 Gönderiler</a>
            </li>
            <li class="active">
                <a href="/comments">💬 Yorumlar</a>
            </li>
        </ul>
        <div class="sidebar-footer">
            <a href="/logout" class="logout-btn">🚪 Çıkış</a>
        </div>
    </nav>

    <main class="content">
        <div class="page-header">
            <h1>Yorum Havuzu</h1>
            <p style="color:#666;margin-top:5px">Bot, yorum yaparken bu listeden rastgele seçim yapacak</p>
        </div>

        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
            <% } %>
                <% if (success) { %>
                    <div class="alert alert-success">
                        <%= success %>
                    </div>
                    <% } %>

                        <div class="card">
                            <div class="card-header">
                                <h3>Tek Yorum Ekle</h3>
                            </div>
                            <div class="card-body">
                                <form action="/comments/add" method="POST" class="inline-form">
                                    <div class="form-group">
                                        <input type="text" name="text" placeholder="Yorum metni yazın..." required>
                                    </div>
                                    <button type="submit" class="btn btn-primary">Ekle</button>
                                </form>
                            </div>
                        </div>

                        <div class="card">
                            <div class="card-header">
                                <h3>Toplu Yorum Ekle</h3>
                            </div>
                            <div class="card-body">
                                <form action="/comments/bulk-add" method="POST">
                                    <div class="form-group">
                                        <label>Her satıra bir yorum yazın</label>
                                        <textarea name="bulkComments" rows="8" placeholder="Harika bir paylaşım! 🎉
Çok güzel olmuş 👍
Tebrikler! ✨
Bu çok ilham verici..." style="width:100%;"></textarea>
                                    </div>
                                    <button type="submit" class="btn btn-primary">Toplu Ekle</button>
                                </form>
                            </div>
                        </div>

                        <div class="card">
                            <div class="card-header"
                                style="display:flex;justify-content:space-between;align-items:center">
                                <h3>Yorum Listesi (<%= comments.length %> adet)</h3>
                                <% if (comments.length> 0) { %>
                                    <form action="/comments/delete-all" method="POST"
                                        onsubmit="return confirm('Tüm yorumları silmek istediğinize emin misiniz?')">
                                        <button type="submit" class="btn btn-sm btn-danger">Tümünü Sil</button>
                                    </form>
                                    <% } %>
                            </div>
                            <div class="card-body">
                                <table class="data-table">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Yorum</th>
                                            <th>Kullanım</th>
                                            <th>İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% if (comments.length===0) { %>
                                            <tr>
                                                <td colspan="4" class="text-center">Henüz yorum eklenmedi</td>
                                            </tr>
                                            <% } %>
                                                <% comments.forEach(function(comment) { %>
                                                    <tr>
                                                        <td>
                                                            <%= comment.id %>
                                                        </td>
                                                        <td>
                                                            <%= comment.text.length> 60 ? comment.text.substring(0, 60)
                                                                + '...' : comment.text %>
                                                        </td>
                                                        <td>
                                                            <%= comment.usedCount %> kez
                                                        </td>
                                                        <td class="actions">
                                                            <form action="/comments/delete/<%= comment.id %>"
                                                                method="POST" style="display:inline"
                                                                onsubmit="return confirm('Bu yorumu silmek istediğinize emin misiniz?')">
                                                                <button type="submit"
                                                                    class="btn btn-sm btn-danger">Sil</button>
                                                            </form>
                                                        </td>
                                                    </tr>
                                                    <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
    </main>
</body>

</html>