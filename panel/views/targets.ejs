<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MetaSal - Hedefler</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="sidebar">
        <div class="sidebar-header">
            <h2>MetaSal</h2>
        </div>
        <ul class="nav-links">
            <li>
                <a href="/dashboard">📊 Dashboard</a>
            </li>
            <li>
                <a href="/profiles">🖥️ Profiller</a>
            </li>
            <li class="active">
                <a href="/targets">📄 Gruplar/Sayfalar</a>
            </li>
            <li>
                <a href="/posts">📝 Gönderiler</a>
            </li>
            <li>
                <a href="/comments">💬 Yorumlar</a>
            </li>
        </ul>
        <div class="sidebar-footer">
            <a href="/logout" class="logout-btn">🚪 Çıkış</a>
        </div>
    </nav>

    <main class="content">
        <div class="page-header">
            <h1>Sayfa / Grup Hedefleri</h1>
        </div>

        <% if (error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
            <% } %>
                <% if (success) { %>
                    <div class="alert alert-success">
                        <%= success %>
                    </div>
                    <% } %>

                        <div class="card">
                            <div class="card-header">
                                <h3>Yeni Hedef Ekle</h3>
                            </div>
                            <div class="card-body">
                                <form action="/targets/add" method="POST" class="inline-form">
                                    <div class="form-group">
                                        <select name="type" required>
                                            <option value="">Tip Seçin</option>
                                            <option value="page">Sayfa</option>
                                            <option value="group">Grup</option>
                                        </select>
                                    </div>
                                    <div class="form-group">
                                        <input type="url" name="url" placeholder="Facebook URL" required>
                                    </div>
                                    <div class="form-group">
                                        <input type="text" name="name" placeholder="İsim (opsiyonel)">
                                    </div>
                                    <button type="submit" class="btn btn-primary">Ekle</button>
                                </form>
                            </div>
                        </div>

                        <div class="card">
                            <div class="card-header">
                                <h3>Hedef Listesi</h3>
                            </div>
                            <div class="card-body">
                                <table class="data-table">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Tip</th>
                                            <th>URL</th>
                                            <th>İsim</th>
                                            <th>Beğenen</th>
                                            <th>Durum</th>
                                            <th>İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% if (targets.length===0) { %>
                                            <tr>
                                                <td colspan="7" class="text-center">Henüz hedef eklenmedi</td>
                                            </tr>
                                            <% } %>
                                                <% targets.forEach(function(target) { %>
                                                    <tr>
                                                        <td>
                                                            <%= target.id %>
                                                        </td>
                                                        <td>
                                                            <%= target.type==='page' ? '📄 Sayfa' : '👥 Grup' %>
                                                        </td>
                                                        <td><a href="<%= target.url %>" target="_blank" class="link">
                                                                <%= target.url.substring(0, 40) %>...
                                                            </a></td>
                                                        <td>
                                                            <%= target.name || '-' %>
                                                        </td>
                                                        <td>
                                                            <strong>
                                                                <%= target._count.likedBy %> profil
                                                            </strong>
                                                        </td>
                                                        <td>
                                                            <div style="display:flex; flex-direction:column; gap:5px">
                                                                <span
                                                                    class="status-badge <%= target.status === 'completed' ? 'success' : target.status === 'failed' ? 'danger' : 'warning' %>">
                                                                    <%= target.status==='completed' ? 'Beğenildi' :
                                                                        target.status==='failed' ? 'Başarısız'
                                                                        : 'Bekliyor' %>
                                                                </span>
                                                                <span
                                                                    class="status-badge <%= target.isActive ? 'success' : 'secondary' %>"
                                                                    style="font-size:0.7rem; <%= !target.isActive ? 'background:#ccc;color:#333' : '' %>">
                                                                    <%= target.isActive ? 'Aktif' : 'Pasif' %>
                                                                </span>
                                                            </div>
                                                        </td>
                                                        <td class="actions" style="min-width:220px;">
                                                            <div style="display:flex; flex-direction:column; gap:10px;">
                                                                <!-- Yönetim butonları -->
                                                                <div style="display:flex; gap:4px;">
                                                                    <form action="/targets/toggle/<%= target.id %>"
                                                                        method="POST" style="margin:0;">
                                                                        <button type="submit" class="btn btn-sm"
                                                                            style="padding:4px 8px; font-size:0.75rem; <%= target.isActive ? 'background:#6c757d;' : 'background:#28a745;' %> color:#fff; border:none; border-radius:4px;">
                                                                            <%= target.isActive ? '⏸️' : '▶️' %>
                                                                        </button>
                                                                    </form>
                                                                    <form action="/targets/retry/<%= target.id %>"
                                                                        method="POST" style="margin:0;">
                                                                        <button type="submit" class="btn btn-sm"
                                                                            style="padding:4px 8px; font-size:0.75rem; background:#ffc107; color:#000; border:none; border-radius:4px;">🔄</button>
                                                                    </form>
                                                                    <form action="/targets/delete/<%= target.id %>"
                                                                        method="POST" style="margin:0;"
                                                                        onsubmit="return confirm('Bu hedefi silmek istediğinize emin misiniz?')">
                                                                        <button type="submit" class="btn btn-sm"
                                                                            style="padding:4px 8px; font-size:0.75rem; background:#dc3545; color:#fff; border:none; border-radius:4px;">🗑️</button>
                                                                    </form>
                                                                </div>
                                                                <!-- Boost -->
                                                                <form action="/targets/boost/<%= target.id %>"
                                                                    method="POST"
                                                                    style="margin:0; display:flex; gap:4px; align-items:center;">
                                                                    <input type="number" name="postCount" value="4"
                                                                        min="1" max="10"
                                                                        style="width:40px; padding:4px; font-size:0.8rem; border-radius:4px; border:1px solid #444; background:#1a1a2e; color:#fff; text-align:center;">
                                                                    <button type="submit" class="btn btn-sm"
                                                                        style="padding:4px 10px; font-size:0.8rem; background:linear-gradient(135deg,#667eea,#764ba2); color:#fff; border:none; border-radius:4px; white-space:nowrap;">
                                                                        🚀 Boost
                                                                    </button>
                                                                </form>
                                                            </div>
                                                        </td>
                                                    </tr>
                                                    <% }); %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
    </main>
</body>

</html>